# Python_Submission
Task_1.py
 - This Python program performs basic arithmetic operations on two numbers entered by the user. It accepts two numbers as input from the user. The input values are converted into integers to allow mathematical calculations. The program then performs addition, subtraction, multiplication, and division on the given numbers. The results of all four operations are displayed on the screen with clear labels.

Task_2.py
 - This Python program takes the user’s first name and last name as input, combines them into a full name, and then displays a personalized greeting message welcoming the user to learn Python.
